---
- name: Install k3s without Traefik or ServiceLB
  shell: curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="--disable=traefik --disable=servicelb" sh -
  args:
    creates: /usr/local/bin/k3s

- name: Install helm
  shell: curl -sfL https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash
  args:
    creates: /usr/local/bin/helm
